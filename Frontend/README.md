# Frontend

## Overview
The Frontend is responsible for the user interface and user experience of the application. It communicates with the backend to fetch and display data.

## Overview
The Frontend is responsible for the user interface and user experience of the application. It communicates with the backend to fetch and display data.

## Directory Structure
- `src/`: Contains the main source code for the frontend application.
- `public/`: Contains static files like images and icons.
- `node_modules/`: Contains all npm packages.
- `package.json`: Lists dependencies and scripts for the project.
- `src/`: Contains the main source code for the frontend application.
- `public/`: Contains static files like images and icons.
- `node_modules/`: Contains all npm packages.
- `package.json`: Lists dependencies and scripts for the project.

## Installation
1. Clone the repository:
   ```bash
   git clone <repository-url>
   cd frontend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Create a `.env` file if required and configure your environment variables.
1. Clone the repository:
   ```bash
   git clone <repository-url>
   cd frontend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```

## Running the Application
To run the application, use the following command:
```bash
npm start
```

## Running Tests
To run the tests, use:
```bash
npm test
```
To run the application, use the following command:
```bash
npm start
```

## Running Tests
To run the tests, use:
```bash
npm test
```

## License
This project is licensed under the MIT License.
